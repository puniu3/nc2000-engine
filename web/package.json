{
  "name": "nc2000-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 8000",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host --port 8000"
  },
  "dependencies": {
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
