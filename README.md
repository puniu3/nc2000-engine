# nc2000-engine

Rust port of Pokemon Showdown's **`[Gen 2] Nintendo Cup 2000 No OHKO Stadium2 Strict`** format (mod: `gen2stadium2nc2000` — the community server's no-OHKO regulation, the one with the larger player base and the one the bot actually plays on ladder; migrated from the OHKO-allowing `gen2nc2000` on 2026-07-21, see `PORT-NOTES.md`), built to raise bot-research search throughput by orders of magnitude.

**The source of truth is PS as actually implemented.** Divergence from cartridge GSC or real Stadium 2 hardware is out of scope. Correctness is defined as **bit-exact parity** (state + PRNG seed, at every snapshot point) against golden fixtures generated from PS by `tools/gen-fixtures.js`. (One documented exception where PS itself crashes: `crates/conformance/tests/berry_confusion.rs`.)

**Status:** engine bit-exact vs PS (M1–M4), search bots through imperfect-info play (M5–M10), wasm + browser demo shipped (M9), published (M12), Japanese localization (M13). Current phase: **bot/eval sanity verification (M16 — see Roadmap (M16+))**. Playable demo: **https://puniu3.github.io/nc2000-engine/** Licensing: MIT, third-party attribution in [`THIRD-PARTY-NOTICES.md`](THIRD-PARTY-NOTICES.md).

## Layout

```
tools/            Node scripts run against the reference PS build (needs PS_ROOT=PS repo, `node build` done)
  export-dex.js            dump the flattened gen2stadium2 dex into data/
  export-learnsets.js      per-species move-legality sets probed through PS's real TeamValidator (M14a)
  validate-oracle.js       cross-check the wasm validator against PS's TeamValidator (M14a)
  gen-prng-vectors.js      PRNG parity vectors
  gen-fixtures.js          golden fixture generator (live battle -> inputLog replay -> snapshot extraction)
  gen-porting-checklist.js regenerate PORTING.md
data/gen2stadium2.json     reference data (functions replaced by callback-name lists; meta.psCommit records origin)
data/learnsets-gen2.json   per-species format-legal move sets + level floors + HP DV spreads (M14a; flat
                           acceptance sets — cross-move compatibility deliberately not encoded)
data/meta-pool-v0/         curated meta team pool (M8): 34 tournament/expert teams, provenance in its README
data/preview-tables-v0/    baked team-preview equilibria per matchup (M8), format in its README
fixtures/prng-vectors.json PRNG vectors
fixtures/corpus-v1/        60 battles (30 puredata + 30 full; 2,268 turns / 2,585 snapshots)
crates/engine/             the engine (prng / dex / state / choice / battle; battle/search.rs = M3 search
                           API; validate.rs = M14a team validator + canonicalizer)
crates/conformance/        conformance harness (fixture schema, divergence reporter, replay tests)
crates/bot/                bots: random / max-damage / open-loop DUCT MCTS (M5) + heavy playouts, static
                           eval, duel harness, SPSA tuner (M6) + state-keyed tree, RM-solved mixed root,
                           best-response exploitability probe (M7) + baked preview tables, baked/counter
                           agents (M8) + observation tracker, meta-pool belief, hidden-field determinizer
                           (M10a), blind imperfect-info agent (M10b) + its stepped form BlindSearch
                           (M10c), open-team-sheet agent — pinned true-sets belief (M14);
                           examples: arena / play / tune / profile_mcts / bake_preview
crates/wasm/               nc2000-wasm JS bridge (M9): Dex / Battle / Searcher (stepped skuct — the
                           ponder substrate) / PreviewTables / BlindSearcher (M10c per-game imperfect-
                           info agent: observe/step/best + beliefInfo; pinOpponent = M12 open-team-sheet
                           mode) / Validator (M14a validateTeam+canonicalizeTeam, embedded learnsets),
                           JSON-string API, embedded dex; build.sh = tuned wasm-pack build;
                           tests-node/ = parity + bench twins vs native
web/                       Vite+Preact browser demo (M9): worker-threaded bot with ponder, baked-table
                           preview, device benchmark; information policy fixed to OPEN TEAM SHEET and
                           strength fixed at max (M12 — no settings): the bot gets the human's true sets
                           as a pinned singleton belief, only picks stay hidden; custom parties (M14):
                           PS-export paste import (ps-import.ts) -> wasm canonicalize/validate ->
                           localStorage, played under the same open-sheet policy; meta pool + pair
                           tables fetched from <base>data/* at runtime (never bundled — the background
                           bake extends the app in place; the Pages build copies data/ into dist/)
.github/workflows/pages.yml GH Pages build+deploy (M12): wasm build -> vite build (NC2000_BASE=
                           /nc2000-engine/) -> data copy -> actions/deploy-pages
PORTING.md                 porting checklist (377 callbacks, generated)
```

## Verification model (the snapshot contract)

- Fixture `choices` are PS's canonicalized inputLog choice lines (e.g. `team 5, 6, 1` / `move surf` / `switch 2`).
- Snapshot points = **immediately after every input line that grew the battle log**. Each snapshot records `turn / requestState / prngSeed / field / sides` (every mon's HP, status, boosts, PP, volatiles) plus the log lines produced since the previous snapshot.
- `prngSeed` uses PS `Gen5RNG.getSeed()` format (four decimal 16-bit limbs, comma-joined). **Seed equality = RNG-consumption-order equality** — a drift in consumption order is caught immediately even when outcomes happen to match.
- Nondeterministic `|t:|` wall-clock lines are stripped at generation time.

## Workflow

```bash
# all tests (green: PRNG parity, dex load, fixture schema, both corpus replays)
cargo test
# adversarial soak: generate fresh fixtures with any seed and sweep them
node tools/gen-fixtures.js --n 100 --pool full --out /tmp/soak --seed 12345
cargo run -p conformance --example sweep -- /tmp/soak
# drill into one diverging fixture (per-choice log diff + seed check)
cargo run -p conformance --example debug -- /tmp/soak/battle-042.json [from_snapshot]
# throughput benchmark (turns/s, playouts/s, ns/clone, allocs)
cargo run --release -p conformance --example bench
# bot arena; deterministic per --seed; prints per-agent think ms/move
# agents: random | maxdamage | mcts[:iters[:c[:eps[:turns]]]] (M6 heavy) | mcts5[:iters[:c]] (M5 baseline)
#         rm[:iters[:probe[:threshold[:buckets]]]] (M7 mixed) | skuct[:iters[:c[:buckets]]] (M7 ablation)
#         blind[:iters[:c[:buckets]]] (M10b imperfect-info skuct: public info + meta-pool
#         prior only, per-iteration belief determinization; outer battles auto-run log-on)
#         open[:iters[:c[:buckets]]] (M14 open-team-sheet product agent: the blind
#         machinery with the opponent's TRUE sets pinned — picks stay hidden; preview by
#         public-signature table lookup, else pinned live search; log-on like blind)
#         exploit:<inner> (best-response probe with a frozen <inner> policy oracle)
#         baked:<inner> | bakedarg:<inner> (M8 table preview, mixed/argmax) | counter[arg]:<inner>
#         (M8 counter-picking probe); teams via --pool fixtures|meta[:LO-HI], tables via --tables DIR
cargo run --release -p nc2000-bot --example arena -- mcts:1000 maxdamage --games 100
cargo run --release -p nc2000-bot --example arena -- rm:1000 mcts:1000 --games 200
cargo run --release -p nc2000-bot --example arena -- baked:skuct:300 skuct:300 --pool meta:0-3 --games 400
# M8: bake preview tables (resumable per pair; deterministic at any thread count)
cargo run --release -p nc2000-bot --example bake_preview -- --teams 0-33
cargo run --release -p nc2000-bot --example bake_preview -- --summarize   # gate report over baked pairs
# M11a: metagame research — (1+λ) mutation hill-climb over the legal set space (resumable
#        per-lineage checkpoints in --out); certification = bake a candidate team FILE vs the
#        tournament tier into a SEPARATE table dir (never data/preview-tables-v0)
cargo run --release -p nc2000-bot --example research_meta -- --budget-profile smoke --seed-team 3 --weaken 4 --out tmp/research-smoke
cargo run --release -p nc2000-bot --example research_meta -- --budget-profile full --seed-team 0 --out data/research-v0 --resume
cargo run --release -p nc2000-bot --example bake_preview -- --candidate data/research-v0/lineage-pool-00.json --vs 0-7 --out data/preview-tables-research
# play the bot yourself in the terminal (or spectate: bot vs bot)
cargo run --release -p nc2000-bot --example play -- human mcts:1000
# SPSA self-play tuning of the eval weights (weights file rewritten every iteration)
cargo run --release -p nc2000-bot --example tune -- --iters 120 --games 48 --mcts-iters 300 --out w.txt
# profile the heavy-playout MCTS workload (writes target/flamegraph-mcts.svg + self-time table)
cargo run --release -p nc2000-bot --example profile_mcts -- 1000
# M15a: play full games against a PS-hosted battle (PS clone at ~/pokemon-showdown, `node build` run;
#        the bot sees only its player stream; every decision point is asserted vs PS's true state)
node tools/ps-arena.js --games 50 --mode blind --opp random --oppteams mixed --iters 1000 --seed 21
# M15b: the same bot over a real PS server websocket (start the clone's server, then two clients =
#        full games: one accepts challenges, one challenges. NOTE: botting on the main ladder
#        (play.pokemonshowdown.com) requires PS staff permission — point --server at your own server)
(cd ~/pokemon-showdown && node pokemon-showdown start --skip-build --no-security 8123)
node tools/ps-client.js --server ws://127.0.0.1:8123 --name rand1 --team pool:random --accept any --games 24 --random
node tools/ps-client.js --server ws://127.0.0.1:8123 --name bot1 --team pool:random --challenge rand1 --games 24 --iters 1000
#        extras: --timer (battle timer on), --mode open --opp-team-file F (pinned sheets),
#        --drop preview:pre,move4:pre,fs:pre (reconnect chaos: kill the socket at those decision
#        points; auto-resume rebuilds from the replayed room log and proves stateView bit-identity)
# wasm (M9): tuned build (fat LTO + wasm-opt -O3; native profile untouched), parity, throughput
crates/wasm/build.sh nodejs && node crates/wasm/tests-node/parity.js
node crates/wasm/tests-node/bench.js     # wasm iters/s; native twin: -p nc2000-wasm --example native_bench
# browser demo (pkg-web via crates/wasm/build.sh): dev server, or typecheck+build+serve the dist
cd web && npm run dev                    # 0.0.0.0:8000 (auto-bumps port if busy)
cd web && npm run build && npm run preview   # <base>data/* served from repo data/ in dev AND preview
# GH Pages build locally (same as .github/workflows/pages.yml; serves at /nc2000-engine/)
cd web && NC2000_BASE=/nc2000-engine/ npm run build && NC2000_BASE=/nc2000-engine/ npm run preview
# M14a: regenerate the learnset export, then cross-check the wasm validator
# against PS's real TeamValidator (needs the pkg-node build from build.sh)
node tools/export-learnsets.js
crates/wasm/build.sh nodejs && node tools/validate-oracle.js
# regenerate artifacts (e.g. after a PS update)
node tools/export-dex.js && node tools/gen-porting-checklist.js && node tools/dump-callbacks.js
node tools/gen-fixtures.js --n 30 --pool puredata --out fixtures/corpus-v1/puredata --seed 100
node tools/gen-fixtures.js --n 30 --pool full     --out fixtures/corpus-v1/full     --seed 200
```

Porting loop: port one callback → tick it off in `PORTING.md` → keep the replay test green as the legal pool grows. On divergence, `compare::Divergence` auto-localizes to the first differing snapshot + JSON path + that turn's log lines.

### Search API (M3)

`Battle` is a plain deep-clonable value; DUCT/MCTS drives it like this:

```rust
let mut b = Battle::from_fixture(&dex, seed, &p1, &p2)?;
b.set_log_enabled(false);              // search mode: skip the protocol log
while b.outcome().is_none() {          // Some(P1Win|P2Win|Tie) when over
    let picks = [0, 1].map(|s| {
        let cs = b.legal_choices(&dex, s);   // empty ⇔ side owes nothing
        cs.first().copied()                  // SearchChoice is Copy
    });
    b.apply_choices(&dex, picks)?;     // advances to the next request point
}
```

Determinized playouts: clone the battle, `clone.reseed(sample)` — the PRNG is
part of the state. `SearchChoice::to_input` renders the PS-canonical choice
string (`move surf` / `switch 3` / `team 5, 6, 1`) for interop with fixtures
and the debug tools.

### Milestones

1. **M1 — puredata corpus green: DONE (2026-07)**. Team init, team preview, switching, the full turn engine (queue/speed ties/PRNG parity), gen2stadium2 damage pipeline, residuals, and the core conditions (statuses, confusion, flinch, partiallytrapped, mustrecharge, weathers, sleep/freeze clauses) replay all 30 puredata battles bit-exact (state + PRNG seed + protocol log at every snapshot).
2. **M2 — full corpus green: DONE (2026-07)**. All 88 callback moves, all 38 callback items, every condition reachable in this format, and the runtime rules. Verified beyond the golden 30: 350 additional fresh-seed battles replay bit-exact (see the soak workflow above); the last 100 diverged nowhere. Every reachable `PORTING.md` entry is ticked; unreachable entries are documented there.
3. **M3 — search API: DONE (2026-07)**. `crates/engine/src/battle/search.rs`: `SearchChoice` (compact/`Copy`), `Battle::legal_choices` / `apply_choice(s)` / `needs_choice` / `outcome` / `reseed`, plus `set_log_enabled(false)` for protocol-log-free stepping. Enumeration mirrors the `choices.rs` validation rules and application funnels through `Battle::choose` with PS-canonical strings — one code path shared with fixture replay. Verified by `tests/search_api.rs`: at every decision point of all 60 golden fixtures the PS choice is inside the enumerated set and (sampled) every enumerated choice is accepted; log-off replay is state+seed-identical; random playouts terminate. A 100-battle fresh-seed soak stayed bit-exact after the M3 perf work.
4. **M4 — throughput: DONE (2026-07)**. Both prescribed structural fixes landed end-to-end: (a) integer event/callback identity — `Cb` ids + `CbMask` bitsets, per-event `Ev` statics with precomputed prefix callbacks, precomputed handler Order/Priority/SubOrder, aggregate handler masks per Pokemon/Side plus one battle-level union mask that lets zero-handler runEvents return without any machinery; (b) flat state — `TypeId`/`TypeList` with effectiveness matrices, Copy `EffectState` (`EffId`/`DK`/compact `Scalar`), inline move slots/name/gender, interned move targets + flag bitmasks, log-only formatters gated off in search mode. Measured (same machine): **54k turns/s replay, 103k turns/s clone-based playouts (the MCTS workload), 4.7 µs / 4-alloc clones** — 5.3x / 4.7x / 38x-fewer-allocs vs M3. Verified: full corpus bit-exact + 420 fresh-seed battles (3 soak batches), which also caught and fixed two real M2 gaps (frz thaw on Tri Attack's rolled burn; substitute's onHit sub-cost, reached for the first time in ~600 soak battles). Remaining ideas toward 1e6 (fn-pointer dispatch tables keyed by (CondId, Cb), direct SearchChoice application without the string round-trip, construction diet) are deferred until bot-side search actually wants them.
5. **M5 — baseline bots + arena: DONE (2026-07)**. `crates/bot`: `Agent` trait, `RandomAgent`, `MaxDamageAgent` (static base power x STAB x type effectiveness, no voluntary switches), and `MctsAgent` — **open-loop DUCT** (decoupled UCB1 per side; statistics keyed by `SearchChoice` maps because chance can change even a node's *request kind* between iterations; every iteration re-simulates from a fresh root clone with `reseed` = the determinized-playout pattern; uniform-random rollouts; HP-fraction eval squashed into [0.25, 0.75] at the horizon). `examples/arena.rs`: seed-paired side-swapped duels over the 120 fixture teams, deterministic for a given `--seed` at any thread count. Measured (95% CI): maxdamage > random **0.770±0.058** (200 games); mcts:1000 > maxdamage **0.830±0.074** (100 games); mcts:100 *loses* to maxdamage at 0.450±0.098 — search strength scales with budget (the 120-action team-preview root is undersampled at 100 iterations). Budget-scaling ladder (adjacent-tier duels, 60–100 games): 300v100 **0.775**±0.082, 1000v300 **0.710**±0.089, 3000v1000 **0.580**±0.097, 10000v3000 **0.567**±0.126 — per-3x-budget gain decays ~215 → 156 → 56 → 47 Elo, i.e. the curve enters its saturation band around 3k–10k iterations with uniform rollouts. Further strength must come from rollout/eval quality, not budget. Throughput: mcts:1000 plays ~0.6 games/s over 12 threads. Known levers when more strength is wanted: heuristic (ε-greedy max-damage) rollouts, team-preview candidate pruning, tree reuse across decisions, root parallelization, and the deferred M4 perf ideas.
6. **M6 — strength core (rollout/eval quality): DONE (2026-07)**. `crates/bot/src/eval.rs`: weighted static eval — per living mon HP fraction + alive bonus + status penalties, per-stage boost values on the active, PP fraction, and a **threat feature** (best expected hit fraction vs the opposing active: gen-2 damage core on *effective* stats via `get_stat`, so boosts/burn/screens/boost items feed in; STAB x effectiveness x mean roll x mean multi-hit x accuracy; hidden power uses the real DV-derived type/power) — linear in tunable `EvalWeights`, sigmoid on the side differential, leaf squashed into (0.25, 0.75). `Playout::Heavy` in the MCTS: **ε-greedy max-damage rollouts** (ε=0.2, sharing the damage estimate) **truncated 8 turns** past the rollout start, eval leaf; the M5 agent survives bit-identical as `Playout::Uniform` (arena spec `mcts5`), verified game-for-game: mcts5:1000 vs maxdamage (seed 1) = 82W/18L, exactly what the unmodified M5 commit produces in a worktree (the M5 entry's 0.830 was recorded from a pre-commit state; the committed code's figure is 0.820±0.076). Hyperparameters settled head-to-head (truncation 8 > 4 at 0.683, > 16; ε 0.2 ≈ 0.1 — equal-iteration duels vs a third opponent had pointed the other way, a lesson: compare variants directly). Weight tuning: two SPSA self-play runs over the shared duel harness (`examples/tune.rs` + `duel.rs`; 120 iters x 48 games at mcts:300 and 160 x 96 at mcts:100 with larger perturbations) both held out at no gain vs the hand-written weights (0.490±0.069 / 0.483±0.057) and drifted every weight <15% — the hand values sit on a plateau at arena noise levels; they ship as `EvalWeights::default()`. **Gate passed: mcts:1800 vs mcts5:1000 = 0.665±0.066 (200 games) at equal-or-cheaper wall-clock — 399 vs 484 ms/move measured in-duel** (the duel harness now reports per-agent think time; equal-iteration mcts:300 vs mcts5:300 is 0.650±0.122, and heavy is ~4x cheaper per iteration). Ladder re-measured (heavy adjacent tiers): 300v100 0.670±0.093, 1000v300 0.690±0.091, **3000v1000 0.717±0.115** (M5: 0.580), 10000v3000 0.567±0.126 — the gain per 3x budget now holds ~120–160 Elo through 3k and the saturation knee moved a full tier (~1k–3k → ~3k–10k). Post-truncation profile (`examples/profile_mcts.rs`) is flat — top item `run_event` 9.3%, the whole eval+policy adds ~1.7% — so the deferred M4 perf ideas stay deferred.

7. **M7 — mixed strategies: DONE (2026-07). Parity gate passed; exploitability gate measured null.**
   Engine: `Battle::state_key()` / `state_key_bucketed(b)` — a drift-proof (totally-destructured,
   so adding a state field breaks the build until placed) hash of every decision-relevant field,
   PRNG excluded, with HP and roll-magnitude bookkeeping optionally bucketed; inline FxHash-style
   hasher (SipHash cost 1.5x'd think time before). Bot (`crates/bot/src/smmcts.rs`): state-keyed
   transposition-table tree over bucketed keys — chance splits on anything discrete (KOs, status
   procs, request kinds, durations) so nodes have stable request kinds and cached legal-action
   sets, while HP pools into 16 buckets (exact keys measured weaker: every damage roll becomes
   its own node and the tree starves of depth) — decoupled UCB1, and a two-phase budget: 75%
   tree, 25% probing the top-3×top-3 root joint cells (seeded with the late tree half's on-policy
   samples), whose payoff matrix is solved offline by full-width RM+ with linear averaging;
   play samples the thresholded average strategy and defers to argmax-visits at solver-pure
   spots. Rejected by measurement en route (scores vs maxdamage where mcts:300 = 0.82):
   online outcome-sampling RM at every node 0.30–0.43 (IS spikes up to |A|/γ + flat exploration
   tax), online root-only RM 0.50–0.58, RM+ under sampling (regret-clamp ping-pong), EMA cell
   estimates over sparse probes, RM team previews (120 actions is outside the sampled-equilibrium
   regime; preview stays UCB+argmax — M8 bakes it offline anyway). **Parity gate (200 games each,
   seed 1, equal iterations AND equal wall-clock — e.g. 230.6 vs 228.4 ms/move):** rm:1000 vs
   mcts:1000 = 0.475±0.069, rm:3000 vs mcts:3000 = 0.480±0.069, argmax ablation skuct:1000 =
   0.460±0.069, skuct:3000 = 0.525±0.069 — every CI straddles 0.5: the state-keyed tree replaces
   open-loop aliasing at zero strength cost, and the mixed layer costs nothing measurable on top.
   M5/M6 agents preserved bit-identical (mcts5:1000 vs maxdamage seed 1 reproduces 82W/18L
   exactly). **Exploitability gate: null.** Probe = `exploit.rs` best response with a
   seed-marginal (3-sample) policy oracle at 3x budget — a single-sample point-mass oracle is
   neutralized by MCTS seed noise (0.480, no exploitation; an "argmax" policy is only
   deterministic per rng seed). At 200-game resolution the probe gains +0.118±0.067 vs frozen
   mcts argmax, +0.110 vs skuct argmax, +0.100 vs rm mixed — indistinguishable; heavier mixing
   (threshold 0.15) made the target MORE exploitable (+0.135), and the argmax-tuned BR beat the
   mixed agent hardest (0.680). Reading: mixing from an estimated matrix is not equilibrium
   mixing — against non-adapting opponents it only leaks play quality, and instance seed noise
   already provides free mixing. The RM layer and the `Agent::root_policy` oracle API stay for
   when an adapting opponent exists (M8 human play, M10 hidden info); the strength
   recommendation after M7 is the state-keyed argmax agent (`skuct`), which is also the natural
   substrate for M10's determinized belief states.

### Roadmap (M8+)

Scope decisions (2026-07, settled with the owner):

- **Mixed strategies / per-decision GTO approximation: IN.** Exploiting weak opponents: OUT — no opponent modeling beyond the equilibrium baseline.
  - Post-M7 refinement (2026-07, settled with the owner): in-battle online mixing measured null (see M7) and is parked; **GTO ships at the team-preview layer in M8** (offline-solved equilibrium over the meta matchup matrix — exact cells, real equilibrium, and the adapting adversary — humans studying the tables — actually exists there) and returns in-battle at M10 where mixing has a structural job (hidden-information leakage). In-battle flagship until then: `skuct` (state-keyed argmax).
- **Imperfect-information play is a gradient goal**: pursued after the strength core lands. The perfect-info bot is never discarded — it stays as dev harness, teacher, and upper-bound benchmark.
- **Ship target: Rust→wasm, client-side, no GPU anywhere.** No hard latency cap (the player trades wait time for strength; ponder hides most of it). Dev-time compute baked into shipped tables is allowed and encouraged.
- **Mainline meta battles: IN. Minor/casual party robustness: OUT** — priors, tables, and evaluation specialize on a curated meta pool.
- **Auto team building: stretch** — big completeness win, deferred unless it turns out cheap. *(Superseded 2026-07-17: owner-requested — see M11.)*
- **Post-M10 product decisions (2026-07-17, settled with the owner after iPad playtesting; the bot now exceeds the owner's strength and think time was a UX non-issue):**
  - **Strength fixed at max** — the 1k/3k/10k/30k selector goes away (lands in M12); ponder + measured iPad speed (0.23 s/move at 3k) make the wait invisible.
  - **The public build's information policy is fixed to OPEN TEAM SHEET** (VGC-style): both sides' full sets are public — the player can read the bot's pool team in the team list; the bot receives the player's exact sets (custom parties included) as a singleton belief — and only *selection* (which 3 of 6 + lead, hidden until revealed) stays private, uniformly for pool and custom parties. Rationale: (i) table-optimized selection against pool teams while custom teams face weaker live-search selection would punish choosing a pool team and read as "pool = leaked to the bot" unfairness; (ii) full-blind instead makes off-meta gimmick parties that farm belief blind spots the dominant strategy — against the game's spirit; (iii) open sheet keeps one policy for everyone, matches the M8 tables' solution concept exactly (their equilibrium already assumes both sides know the matchup), and keeps the most player-visible tell (perfect switch-ins against unrevealed benches) dead via hidden picks. The Fair/X-ray toggle is removed from the public build; the full blind machinery stays for arena/research and becomes the product policy where sets are genuinely hidden (M15 PS play).
  - **Custom parties: IN** (revises "minor/casual party robustness: OUT" — robustness *guarantees* stay out of scope, but custom input is accepted and gets the same information policy and selection quality path).

Milestones:

8. **M8 — meta pool + baked tables: DONE (2026-07-16). Both gates passed.** Plan as approved: curate the NC2000 meta team/set pool (replaces the random fixture pool as the product distribution), then bake dev-time compute into shipped tables: **team-preview policy per matchup as an offline-solved (mixed) equilibrium** over the meta matchup matrix, solved with M7's full-width RM+ solver — this is where mixed strategies ship, because the estimates are offline-exact and the adapting adversary (humans studying the tables) exists. In-battle search: `skuct`.
   **Meta pool v0: DONE (2026-07-16, curation delegated to mechanical tournament-pedigree criteria after the owner opted out of meta judgment).** `data/meta-pool-v0/`: 34 PS-validator-clean teams — 8 tournament-proven (Historia Cup 7.5 top-8, the last live tournament on NC2000 rules proper; later Historia Cups use an incompatible special ruleset) + 26 expert-built samples (Smogon NC2000 Resource Hub, intl+JP authors) — ranked by placement → Chio-VR mean → HC7.5 usage mean, all components in the JSON for downstream re-weighting. `tools/build-meta-pool.js` regenerates from `raw/`; conformance test `meta_pool` proves every team plays to completion in the engine. Provenance, exclusions, and expansion sources (Gym Leader's Castle tournament archive etc.): `data/meta-pool-v0/README.md`. Notable: the shipped PS format bans neither evasion nor OHKO nor Bright Powder, so JP tournament sets are legal verbatim.
   **Baked preview tables: DONE (2026-07-16).** `crates/bot/src/preview.rs` + `examples/bake_preview.rs` + `data/preview-tables-v0/`. Preview space quotiented 120 → **60 canonical actions** (20 subsets × 3 leads; bench order only picks which display slot a random drag-in lands in — uniform either way, so win probability is invariant — 4x fewer matrix cells). Per pair: full-width 60×60 **screen** with ε-greedy max-damage self-play (16 games/cell ≈ 3 s — ranking signal only, the policy never switches voluntarily), **support** = top screen-equilibrium best responses ∪ `skuct:2000` advisor picks at the real preview root (8/side), **refine** support×support with `skuct:200` self-play (24 games/cell, P1/P2 alternated per game, fresh seeds; ~5 min/pair on 12 threads), **solve** with full-width RM+ → mixed + argmax policies + exact counter-picking guarantees, one resumable JSON per pair, byte-deterministic at any thread count (verified). Consumption: `TableSet` resolves live rosters to pool teams by full-set signature; `baked[arg]:<inner>` plays table preview (mixed sample / argmax) and delegates the battle to `<inner>`, falling back to `<inner>`'s own preview search on unknown matchups; `counter[arg]:<inner>` is the probe that best-responds at preview to a known table policy. **Gate "mixed beats counter-picking" passed (offline, exact):** over the 10 pairs of the top-4 tournament teams, mixed-vs-argmax counter-pick margin mean **+0.077**, max **+0.245**, 0 violations; genuine mixing in 8/10 pairs (mirror equilibria mix up to 6 preview actions; 1st-vs-3rd holds 0.370 mixed where argmax is counter-picked to 0.125). **Gate "baked ≥ online" passed (runtime, equal in-battle budget):** baked:skuct:300 vs skuct:300 = **0.630±0.047** (400 games; argmax tables alone 0.600±0.048, and the baked side is also cheaper per move — preview costs a lookup); at skuct:1000, 0.555±0.069 — the online 120-action preview root improves with budget but does not catch up. **End-to-end counter-pick probe (the optional certification): null at 400-game resolution** — counterarg vs bakedarg 0.460±0.049, counter vs baked 0.495±0.049; the matrix-exact exploitation edge (~+0.08 predicted) does not survive 24-game/cell estimation noise + BR winner's curse + instance seed noise at product budgets. Same reading as M7: mixing's measurable value lives at the matrix level (where the gate is exact and the table-studying adversary operates), and it costs nothing in play (0.630 vs 0.600 vs the same online opponent). Full-pool bake (595 pairs, ≈2 days) left running resumably (`--teams 0-33`, tournament teams first; `tmp/bake-full.log`); the pilot 10 pairs ship with this commit and the directory extends in place. Eval weights stay at the M6 plateau conclusion; "opening lines" are dropped — baked previews + `skuct` in battle cover the opening. Preview-time team identification vs humans (species+levels only; one species-set collision in the pool) is deferred to M10's belief machinery — the wasm client falls back to online preview until then.
   **Correction (2026-07-17): the preview space lacked the format's Max Total Level rule.** Found by M15a's live-PS harness: PS enforces at team preview that the PICKED 3 mons' level sum ≤ 155 — certified against the live clone (`tools/probe-max-total-level.js`): inclusive threshold (155 OK, 156 rejected), rejection is a choice error (the request stays open), lead order irrelevant, and a team with no legal triple cannot exist (validator lowest-3 check). The engine's preview validation/enumeration did not enforce it, so the baked 60-action space included PS-illegal picks for **20/34 pool teams** (1–10 of 20 subsets each) touching **490/595 pairs**. Fixed in the engine (`choices.rs` validation + `search.rs` enumeration, constant `battle::MAX_TOTAL_LEVEL` with the certificate) and the bake (per-side legal action subsets; `space_version` stamped into pair JSON; loading — native `TableSet` and the wasm/web path — rejects stale files as missing so consumers fall back to live preview search; `bake_preview` re-bakes stale files on resume). All pre-fix pair tables (211 baked at fix time, all touching affected teams) were sheltered to `tmp/stale-preview-tables-prefix155/` and the bake restarted on the fixed space. The M8 gate numbers above were measured on the pre-fix space over the pilot pairs; the device benchmark's fixed preview picks also changed (`team 1,2,4`/`team 1,3,4` — the old picks are now illegal), so benchmark numbers are comparable only within the post-fix workload. **Re-certified (2026-07-20) on the completed post-fix full-pool bake (595/595 pairs, `space_version` 1).** Offline gate over the original 10 top-4 pairs: margin mean **+0.152**, max **+0.457**, 0 violations, mixing in 9/10 pairs (pre-fix +0.077/+0.245/0, 8/10). Full-pool `--summarize`: mean +0.119, max +0.591, mixing in 555/595 pairs, one nominal violation (pair 06-11 side a, −0.001) — a shed-dust artifact, not a solve failure: the refined matrix's exact equilibrium passes at +0.006, but the 50k-sweep RM+ average left a genuine ~4.8%-weight support action just under the 5%-of-max dust threshold; the magnitude is far below the 24-game/cell estimation quantum (1/24 ≈ 0.042). Runtime gate (baked:skuct:300 vs skuct:300, 400 games, seed 1): **0.632±0.047** over the full meta pool (all pairs baked), 0.620±0.048 restricted to the pilot meta:0-3 pool (pre-fix 0.630±0.047).
9. **M9 — wasm ship: DONE (2026-07-17). Gate passed, certified on-device (iPad: 12,987 iters/s ⇒ 0.23 s/move at 3k — ~13x inside the envelope).** wasm32 build, JS bridge + table loading, browser demo, ponder. Gate: ≥ native mcts:3000-equivalent strength at 2–3 s/move on the target device, degrading gracefully into longer opt-in thinks — per the M7 parity gate skuct:3000 ≡ mcts:3000, so the gate reduces to "skuct 3000 iterations within 2–3 s in iPad Safari", with the 1k/3k/10k/30k strength selector as the graceful-degradation axis.
   **M9a — wasm bridge: DONE (2026-07-17).** `crates/wasm` (`nc2000-wasm`, cdylib+rlib, wasm-bindgen, JSON-string API; JS names): `Dex` (embedded 416 KB dex, or `Dex.fromJson`), `Battle` (team-JSON × 2 + PS-format seed; `legalChoices` with UI metadata, `applyChoice` by PS-canonical string, `stateView`, `takeNewLog` for GUI narration, `seed` for parity), `Searcher` (persistent stepped skuct over one decision point — `step(n)`/`best`/`rootPolicy` — the ponder substrate; `SkuctSearch` extracted from `RmAgent`, which now drives it internally: arena sanity runs reproduce pre-refactor W/L game-for-game for both skuct and rm), `PreviewTables` (`TableSet` fed meta-pool + pair JSON as strings; `resolve`/`sample`/`argmax`). Node tests (`crates/wasm/tests-node/`, plain node against the nodejs-target build): full searcher-driven game to completion; 3-fixture replay **bit-exact at every snapshot** (PRNG seed + turn + log chunk) plus final state/outcome; same-seed determinism (identical outcome/log/final seed). Throughput on identical iteration streams (twin bench, `examples/native_bench.rs` vs `tests-node/bench.js`): **wasm 2.2k vs native 2.8k skuct iters/s (79%)**.
   **M9b — browser demo: DONE (2026-07-17).** `web/`: Vite+Preact playable human-vs-bot demo over the bridge (the `play` example is the display-semantics source) — meta-pool team select, interactive team preview (bot preview from the M8 baked tables when the matchup is baked, else live `Searcher` fallback at the preview root), per-decision battle UI (moves with type/category/BP/PP, switches, HP bars/boosts/volatiles, field conditions, narrated protocol log), outcome + rematch. The bot thinks in a Web Worker holding a **lockstep mirror battle** in its own wasm instance (the PRNG is part of the state, so same picks ⇒ identical mirror), pumping `step()` slices with progress posts up to the strength budget; simultaneous request points already overlap bot search with human thinking. Battle data (meta pool + pair tables) is never bundled — fetched read-only from `/data/*` via a shared dev+preview middleware, so the running full-pool bake extends the shipped app in place. Verified against the built dist with Playwright: full games to outcome on baked and unbaked pairs, console clean, one hashed wasm asset shared by main and worker chunks.
   **M9c — ponder + strength gate: DONE (2026-07-17).** *Ponder:* the bot never stops thinking while waiting on the human. At a simultaneous point the required budget runs first (chip: "thinking n/budget"), then the search keeps stepping **bonus iterations** (chip: "pondering +N", live counter) until the human commits — a flush returns `best()` at the next slice boundary — or a 10x-budget cap bounds memory; bot-only points (e.g. faint-forced replacement while the human owes nothing) still stop at budget. Measured with Playwright on the built dist: thinking→pondering flip at exactly the budget, +7.2k bonus iterations 4.5 s into a 1k-budget ponder, and **99 ms commit→advance latency while pondering** — the human never waits at a pondered point. Search slices are time-adaptive (~125 ms target): progress ticks ~7x/s even at a 30k budget. *wasm perf:* dedicated wasm build profile (`crates/wasm/build.sh`: fat LTO + 1 codegen unit as env overrides — native profile untouched — plus wasm-opt -O3 via package metadata): Node throughput 2.29k → **2.41k iters/s** (+5%; -O4 measured equal), binary 1,316,243 → **1,267,978 B** (~360 KB gzipped). Parity re-verified on the tuned binary (3-fixture bit-exact + arena skuct:300 seed 1 reproduces 14W 6L; native suite green). *Device benchmark:* title-screen affordance runs the worker's `Searcher` on a fixed workload (fixed teams + battle/searcher seeds + 10k iterations ⇒ the identical iteration stream on every device, ~5 s) and reports iters/s + projected s/move at 3k with PASS/MISS against ≤3 s. *Gate:* headless chromium against the built dist (this machine) = **2.0–2.2k iters/s ⇒ skuct:3000 ≈ 1.4–1.5 s/move — inside the 2–3 s envelope with ~2x margin** (browser ≈ 90% of Node, ≈ 75% of native). On-device certification (2026-07-17, owner's iPad via the title-screen benchmark): **12,987 iters/s ⇒ 0.23 s/move at 3k — PASS** with ~13x margin (the iPad outruns this machine's browser rate ~6x; even the 30k opt-in strength tier lands at ~2.3 s/move).
10. **M10 — imperfect info: DONE (2026-07-17).** Belief = distribution over the M8 meta sets filtered by observations; per-iteration determinization imputes sampled hidden movesets/items onto the public state. The meta-pool restriction is exactly what makes this cheap. DV-level purity and hidden-counter purity are non-goals. Gates: blind-vs-perfect-info gap measured; no psychic tells at parity budget — both landed in M10b; the browser demo plays blind by default since M10c.
   **M10a — observation + belief machinery: DONE (2026-07-17).** `crates/bot/src/observe.rs`: `Observer` — what one player legitimately knows, accumulated at real decision points over two channels: a state diff over opponent fields whose every engine mutation path is publicly announced (move reveals via base-slot PP marks, item transitions; every path audited in the module doc), plus an incremental protocol-log scan for the trace-free reveals (Leftovers heals, Focus Band procs, Sleep-Talk-called moves) when the outer battle runs log-on. Conservative by construction: a missed reveal leaves the belief a superset, never false-filters. `belief.rs`: `Belief` — uniform candidates over the meta pool, preview-filtered on public species/levels/item-presence (the pool's one species+level collision keeps 2 candidates alive), in-battle-filtered on reveals, synthesized per-mon fallback roster for non-pool opponents; `determinize` clones the true battle and overwrites everything hidden — sets, DVs/stat exp → stats/HP, unseen pick identities (resampled), the pending-move queue tell at mid-turn switch requests (resampled, Pursuit volatile re-planted) — with total-destructure drift guards on `Pokemon`/`Battle`/`Side` (a new state field fails the build until triaged public/hidden).
   **M10b — blind agent + gate measurement: DONE (2026-07-17).** `crates/bot/src/blind.rs`: `BlindAgent` (arena/play spec `blind[:iters[:c[:buckets]]]`) — the `skuct` flagship search with every iteration run on a fresh `Belief::determinize` sample instead of the true state. Root structure: the *own* root action is one global UCB over the public choice list, aggregated across all determinizations (the information set; forced into each iteration), while the opponent's root action and everything below live in per-determinization state-keyed nodes — each determinization's cached legal actions match its own imputed moveset (a shared root panics the moment two candidates disagree on the active's moves); decoupled UCB where the opponent is modeled knowing their own team. Play = argmax visits on the global stats. `run_iteration` is shared verbatim with `SkuctSearch` (generalized to per-side root forcing + start node; existing agents bit-identical — skuct:300 vs maxdamage seed 1 still 14W 6L, wasm parity suite green on the rebuilt binary). Team preview: the opponent's pool team is publicly identifiable by species+levels except for the collision pair, so with exactly one candidate alive and a baked pair available the M8 mixed table plays (belief-mediated index lookup, never the opponent's hidden set signature); otherwise the determinized preview search. Blind arena battles auto-run log-ON (`DuelSpec::log_on`) for the observer's trace channel. En route, fixed an M10a determinizer bug the belief tests couldn't reach: pick-identity resampling corrupted party/position coherence when two never-appeared picks cross-swapped (OOB switch_in under search load; coherence now asserted in the belief tests). **Determinization overhead ≈ 2%** at equal budgets (think ms/move blind vs skuct: 328 vs 322 at 1000 iters, 1041 vs 1022 at 3000) — determinize is clone-scale against a playout-scale iteration, so no caching was warranted. **Gate A — blind-vs-perfect-info gap measured (300 games each, meta pool, seed 1, equal budgets):** blind:3000 vs skuct:3000 (the perfect-info opponent sees blind's exact sets) = **0.443 ± 0.056** — a real but small handicap (≈ −40 Elo); anchor blind:3000 vs maxdamage = **0.897 ± 0.035** (cf. mcts:1000's historical 0.830 ± 0.074) — blind sits at full search strength on the absolute scale. Reading: in-distribution the gap is structurally small — preview species+levels usually collapse the belief to the single true candidate, so determinizations equal the truth up to unseen pick identities and mid-turn pending-move scrubs; the machinery's untested-by-Gate-A value (fallback beliefs vs off-pool humans, collision ambiguity) is what Gate B pins. **Gate B — no psychic tells at parity budget: PASSED** (`crates/bot/tests/blind.rs`, a permanent deterministic regression net): the blind agent (fixed seed, fixed budget) plays P1 against BOTH collision-pair teams (2 vs 8; display order aligned — bench order is the builder's free choice, and the preview filter is order-insensitive for the same reason) as the hidden truth, the opponent scripted to choices legal under both truths; public agreement is certified per decision point by bit-equality of fixed-candidate+fixed-rng determinizations of the two true battles (stronger than log equality — it pins everything the search can see). **18 identical-decision checkpoints (2 previews + 16 in-battle with both candidates alive) across 2 battle seeds, zero divergence**; the net has teeth — an injected perfect-info leak is caught at turn 2.
   **M10c — web integration (default-fair bot): DONE (2026-07-17).** `BlindSearch` — the blind search's stepped form, extracted the way M9a extracted `SkuctSearch` (global information-set root + per-determinization subtrees behind `step`/`best`/visit stats; the per-game belief/observer passed per call), with `BlindAgent` rewired on top bit-identically: Gate B green unchanged, and arena blind:300 vs skuct:300 (20 games, meta pool, seed 1) reproduces **9W 11L, avg turns 15.8** exactly before vs after the refactor. wasm bridge: `BlindSearcher` — one instance per GAME (it accumulates the M10a observer/belief; the observed battle runs log-ON for the trace-free reveal channel), constructed with battle + side + meta-pool JSON, baked pair tables fed via `addPair`, then per decision point `observe(battle)` (re-filters the belief and snapshots a fresh stepped search; at a preview whose belief is a singleton with a baked pair, the M8 mixed table answers via `bakedPreview()` — resolved through the belief, never the opponent's hidden signature) and `step`/`best`/`rootPolicy` exactly like `Searcher`, plus `beliefInfo()` (candidate count / fallback flag / alive pool ids) for the UI. Node smoke drives a full blind game (baked preview via the belief, the true team never filtered, fallback belief vs an off-pool team); parity/determinism suites green on the rebuilt binaries. Web demo: **the bot plays blind by default** — settings gain a "Bot information" toggle, Fair (blind, default: the bot sees only what a human opponent would) / X-ray (perfect info, the M9 searcher); in fair mode the worker's mirror battle runs log-ON, bot preview is decided in the worker (the main-thread `PreviewTables.resolve` path — a perfect-info lookup over both rosters — is x-ray-only), the blind searcher ponders through the same step-slice/flush loop, and the bot's belief shows as a subtle "bot's read: *team / N candidates / custom team*" chip in the battle header and preview screen. Verified on the built dist with Playwright: full fair game to the outcome banner, console clean; thinking→pondering flip intact in blind mode; belief chip live — singleton read (the true team id) on a normal pool matchup with the baked-table preview note, "2 candidates" + live-search preview when the human plays a collision-pair team (no psychic tell despite the baked pair), collapsing to the true team the turn a distinguishing move (Zapdos Substitute) is revealed. The team select is pool-only, so the fallback (custom-team) belief path is certified by the native/Node tests instead.
11. **M11 — bot metagame research (auto team building): CUT (2026-07-21, owner decision).** The full run and its certifications are void; whether metagame research returns to scope is re-decided only after the bot itself is complete and stably operating (see Roadmap (M16+)). The M11a machinery below stays in-tree as a working asset. *Historical entry:* trigger met 2026-07-17 (owner-requested: have the bots research the metagame and discover strong parties). Self-play round-robin + mutation hill-climbing over the *legal* set space, seeded by the meta pool and the finished 595-pair value matrix (the bake output doubles as the meta landscape); resumable background jobs in the bake's mold. Depends on M14a's learnset/legality machinery (mutation operators need it) and on the full-pool bake finishing first (CPU). Discovered teams feed back: pool extension → incremental bake → Pages refresh. Gate: a discovered team whose exact offline equilibrium value against the tournament-tier field beats the curated teams', and which holds up in live arena play.

    **M11a — research harness (machinery only): DONE (2026-07-17).** Built and smoke-tested under tiny budgets while the post-155-fix full-pool re-bake owned the CPU; the multi-day research run itself is deliberately NOT launched (post-bake session). *Mutation operators* (`crates/bot/src/teamgen.rs`): seven neighbor proposers over the legal set space — move swap from the species' learnset (typed Hidden Power resolves to a random type, DV spread re-derived), item swap from the format's item lens (meta-pool items weighted 3:1 over the full dex list, Item Clause honored), level tweak within `[species floor, 55]`, DV tweak (Atk/Def/Spe/Spc; HP DV / gender / shiny / HP-type re-derived), stat-exp tweak (Spc mirrored), species swap over the 246 legal species (set re-derived: a meta-pool set of that species when one exists, else a heuristic default — top damaging moves by bp×STAB with type dedupe + one utility move), and whole-mon replacement from the pool's 204 sets. Every draft is repaired through `canonicalize_team` and must re-validate with **zero findings** — illegal proposals are rejected, never emitted; deterministic per rng seed. Tests (`crates/bot/tests/teamgen.rs`): 1000 proposals from 4 seeds × 3 parents (a T1 team, a sample team, a deliberately weakened team) → 100% validator-clean, level-sum rules independently asserted, all 7 operators fire, ≥50% distinct, seed-reproducible; random legal teams (55/55/50/50/50/50 template) validator-clean. *Fitness* (`teamgen::gauntlet_eval`): seed-paired skuct-vs-skuct duels (both orientations per battle seed) against a gauntlet — default the 8 tournament-tier teams — full games from team preview (live search; candidates have no baked tables), games/opponent and agent budget configurable, bit-identical at any thread count (job list + atomic cursor + job-ordered aggregation, certified by test). *Driver* (`examples/research_meta.rs`): (1+λ) hill-climb, resumable in the bake's mold — one JSON checkpoint per lineage (parent/best team, fitness, full eval history, rng state; atomic tmp+rename writes), `--resume` continues mid-lineage and skips completed ones, `--budget-profile smoke|full`. **Acceptance is paired under common random numbers**: one eval seed per iteration shared by a fresh parent re-eval and all λ children — the first smoke run accepted against a stale parent record and its own held-out verification exposed the ratchet as noise-chasing (in-loop trajectory 0.188→0.500 while the "best" team verified at 0.250 vs the seed's 0.500), so the driver re-evaluates the parent every iteration and a final verification pass re-scores seed and best at 3× games on a shifted seed. *Measured signal* (skuct:100, T1 gauntlet): the fitness separates a pool team from its deliberately weakened form (items stripped + 4 best-move downgrades via the operators) by **0.469 vs 0.383 at 192 games** on identical held-out seeds — but NOT reliably at 16–64-game samples (per-seed swings ±0.14), which sizes the budgets: the hill-climb is a candidate *proposer*; the certification bake is the arbiter. Smoke hill-climbs (weakened `hc75-4th-kg`, eval 32 games, λ=3, 6 iters, minutes-scale): with mild weakening (items + 4 downgrades) 5 of 6 iterations accept a child strictly above that iteration's fresh same-seed parent eval (accepted values 0.344/0.406/0.469/0.344/0.406 from seed 0.250) but the held-out best re-scores 0.260 vs the seed's 0.406 — best-of-λ selection at 32-game evals still picks noise (the noise floor above), correctly refused by the verification pass; with hard weakening (items + 12 move-gutting downgrades, seed fitness 0.094) 4 of 6 iterations accept a child strictly above the parent's same-seed eval (species-swap/mon-replace restorations of gutted slots first) and the held-out best verifies directionally above the seed (0.156 vs 0.146, inside noise). Reading: smoke budgets certify the machinery — proposal → paired eval → acceptance → checkpoint/resume → verification — not strength; ranking the ~0.05-scale neighbor effects of real pool teams needs the full profile's samples. *Certification hook* (the gate's second half): `bake_preview --candidate <teamfile>` bakes candidate-vs-pool pairs from a team file (bare sets array / `{"sets"}` / a lineage checkpoint directly) into a **separate** table dir (default `data/preview-tables-research/`, never `data/preview-tables-v0/`), candidate = row player, same screen→support→refine→solve pipeline and resume semantics (pair seeds derive from the candidate id; the pool-pair path is bit-identical to the M8 baker — certified on a tiny pair before/after the refactor). Smoke-certified one pair at reduced budgets (the smoke lineage's checkpoint loaded directly, vs `hc75-1st-show`: equilibrium value 0.458 with 2×2 mixing, gate margins +0.125/+0.042, 93 s at 4 threads — screen 4 / refine 6 / support 4 / skuct 100 / advisor 300×1 against the bake defaults 16/24/8/200/2000×2; `--summarize` verified over the research dir); full-budget certification of a discovered team vs the 8-team field ≈ 1–1.5 h at 11 threads. **Full-run launch plan (post-bake session):** per lineage `cargo run --release -p nc2000-bot --example research_meta -- --budget-profile full --seed-team <i> --out data/research-v0 --resume --threads 10` for the 8 T1 seeds (plus `--random-team --seed 10<k> --lineage rand-<k>` extras) — full profile = 40 iters × (1+6) evals × 64 games at skuct:300 (~11 s/game single-thread) ≈ **5–6 h per lineage on 10 threads**, checkpoints under `data/research-v0/`; then certify the best lineages with `bake_preview --candidate data/research-v0/lineage-<name>.json --vs 0-7` at default (M8) budgets and compare mean equilibrium value vs the curated teams' 595-matrix values (`--summarize` works on the research dir); gate finale = arena games with the discovered team. Existing agents untouched: arena `skuct:300` vs `maxdamage` seed 1 = 14W 6L reproduced, full native suite green (Gate B included), wasm parity green on the rebuilt binary.
12. **M12 — publication: DONE (2026-07-17) — repo public, Pages live at https://puniu3.github.io/nc2000-engine/.** Plan: pre-publication checklist (full-history secrets scan, LICENSE — MIT proposed with PS-data attribution —, internal-path/PII sweep) → owner approval (public is irreversible; every later push/deploy stays per-push approved) → public GitHub repo → GH Pages playable build (static /data pipeline — full tables ≈ 21 MB but fetched lazily per matchup —, base path, deploy workflow). The strength-fixed-at-max and open-team-sheet decisions land here. Missing pairs fall back to live preview search, so publication does not wait on the bake; one redeploy after the bake completes ships the full tables.
    **M12a — pre-publication checklist: DONE (2026-07-17).** Full-history secrets scan (gitleaks) clean, LICENSE (MIT, attribution to Curiosity), THIRD-PARTY-NOTICES.md (PS data / meta-pool provenance / Pokemon IP), README status + licensing, Cargo package metadata, internal-path/PII sweep (PS_ROOT defaults to the homedir clone), build outputs gitignored.
    **M12b — pages pipeline + product policy: DONE (2026-07-17).** *Open team sheet (single policy, Fair/X-ray toggle removed):* `Belief::pinned` (crates/bot) pins the opponent's TRUE sets as a singleton candidate — determinizations equal the truth except unseen pick identities and the mid-turn pending-move scrub, i.e. exactly selection stays hidden; works identically for pool and custom teams (a pinned custom opponent is a real singleton, never fallback). wasm: `BlindSearcher.pinOpponent(teamJson)`; preview in pinned mode = `open_preview_pick` — both sides resolved by public signature, so the baked pair answers with no identification condition (collision pair included), unbaked matchups fall back to the live determinized preview search. The pool-identification blind machinery is untouched (arena `blind` spec, M15): Gate B green, belief/blind/wasm suites green plus new pinned tests (native + wasm + Node smoke open-sheet game), parity 3-fixture bit-exact on the rebuilt binaries, arena skuct:300 vs maxdamage seed 1 = 14W 6L. *Strength fixed at max:* the 1k/3k/10k/30k selector is gone; the bot always thinks 30k with the M9c ponder loop. Device benchmark unchanged as a workload (fixed 10k-iteration stream — cross-device comparable) and now reports iters/s + projected s/move at 30k + the historical 3k≤3s reference gate. *Pipeline:* `.github/workflows/pages.yml` (workflow_dispatch + push to master touching web/crates/data/the workflow; Rust 1.94.0 + wasm-pack 0.13.1 pinned, `crates/wasm/build.sh`, `npm ci && npm run build` with `NC2000_BASE=/nc2000-engine/` — vite `base` is env-driven, runtime data fetches follow `import.meta.env.BASE_URL` — then data/ copied into dist/data/ and actions/configure-pages → upload-pages-artifact → deploy-pages). Verified locally by replicating the workflow steps into a dist served statically under `/nc2000-engine/` and driving it with Playwright: full game to the outcome banner at 30k (think chip budget 30000, ponder past budget at preview), bot preview from the baked table on a baked pair, live-search fallback on an unbaked pair (expected 404 → worker search), no toggle/no belief chip/no selects, zero console errors, wasm + meta-pool + pair JSONs all 200 under the subpath. *Published (2026-07-17, after GH recovery, owner-approved):* repo flipped public, Pages enabled (Source: GitHub Actions), first deploy verified live at https://puniu3.github.io/nc2000-engine/.
13. **M13 — Japanese localization: DONE (2026-07-17).** Plan: UI strings + narration (`web/src/narrate.ts`) + species/move/item JP name tables (finite: 251 species / 267 moves / 62 items, generated once from public data into a fetched JSON). Redeploy.
    **Shipped:** two locales behind a light string-table layer (`web/src/i18n.ts` + `i18n-strings.ts`, no i18n library) — **ja is the default** when `navigator.language` is Japanese, with an EN/日本語 toggle on the title screen persisted to localStorage (document lang/title follow). *Name data:* `data/i18n-ja.json` — official Japanese names for all **251 species / 267 moves / 62 items / 17 types**, keyed by PS id, generated once by `tools/build-i18n-ja.js` from **PokéAPI** (`ja-Hrkt`/`ja` name arrays; species/moves resolved by dex/move number, items by name-derived slug; the 16 typed Hidden Power variants composed locally as めざめるパワー(タイプ); attribution in THIRD-PARTY-NOTICES.md). The 14 gen-2-only items PokéAPI lacks (the old berries, bows, Berserk Gene, generic Mail) were transcribed from Bulbapedia infoboxes and are marked in `meta.manualFills`; the generator asserts full dex coverage. The app fetches the table under `BASE_URL` at startup — never PokéAPI at runtime; any missing name, or the table failing to load entirely, falls back to the English name the wasm bridge already provides. *Narration:* per-locale template tables in narrate.ts behind one `Templates` interface (the EN path is the unchanged CLI-port phrasing); the ja register is the official-game kana style — 「フーディンは サイコキネシスを つかった!」「こうかは ばつぐんだ!」「きゅうしょに あたった!」「○○は たおれた!」 — with per-condition phrasings for the common volatiles (みがわり/こんらん/やどりぎ/アンコール/メロメロ/のろい/かなしばり…), weather start lines, and the raw-line fallback intact for unparsed tags. Statuses, stat/boost chips (攻+1), volatile/field chips, team select, open-team-sheet note, benchmark box, think/ponder chips (思考中/先読み中), and outcome banners are all localized; team ids, tier chips, and provenance lines stay latin by design. *Verified* on the built dist with Playwright (ja-JP context): ja by default, live EN↔JA toggle both ways, full game to the outcome banner with Japanese narration and JP species/move/item names throughout (baked-table preview note included), no horizontal overflow from the wider glyphs, zero console errors; a fresh en-US context still defaults to English.
14. **M14 — custom parties: DONE (2026-07-17)**: (a) learnset export from the PS clone + wasm-side validator (M14a); (b) PS-export-format paste/import UI + the open-sheet play path + the arena measurement (M14b). Custom teams enter play under the open-sheet policy — the bot gets them as a pinned singleton belief, preview vs custom = live search (no table), battle hides picks only.
    **M14a — learnset export + team validator: DONE (2026-07-17).** *Learnsets:* `tools/export-learnsets.js` → `data/learnsets-gen2.json` (124 KB; provenance = `meta.psCommit`): per-species move acceptance probed one move at a time through **PS's real `TeamValidator.validateSet`** under the full gen2nc2000 lens — 246 species (the probe independently re-derives the 5 Uber bans), 11,244 legal (species, move) pairs incl. gen-1 tradeback sources (Snorlax Counter) and Crystal-only moves (Suicune Aurora Beam — the `nc2000movelegality` ruleset exists in PS but is NOT in this format), event-only moves excluded (Event Moves Clause — Gligar Earthquake), plus level floors (Dragonite/Tyranitar `minLevel` 55; 7 `moveMinLevel` entries for level-up moves inside the 51–55 window, e.g. Espeon Morning Sun 52) and the canonical typed-Hidden-Power DV spreads. **Scope caveat (deliberate):** flat per-species sets — cross-move compatibility (incompatible egg-move parents, gen-1-exclusive + gen-2 egg move) is NOT encoded, so the client accepts a small superset of true PS legality, never the reverse. *Validator:* `crates/engine/src/validate.rs` — `validate_team` (machine-readable findings: `severity` error/fix + `code` + params, no prose contract; M14b localizes) and `canonicalize_team` (applies PS's silent normalizations — ability `'No Ability'`, nature `Serious`, gender from Atk DV, level default 55 — plus the derivable errors: HP DV, SpD:=SpA DV/EV mirrors, shiny-from-DVs, typed-HP DV spreads, EV clamps/fills, nickname repairs, duplicate-move dedupe; species/level/move choices and clause conflicts stay user intent). Checks mirror the format's actual rule table (`Picked Team Size = 3`, `Min/Max Level 50/55`, `Max Total Level 155` with PS's two-check semantics incl. the highest-mon-unusable quirk, Species/Item/Nickname Clause, Obtainable, Event Moves Clause) plus the gen-2 landmines: SpA=SpD DV/EV, derived HP DV, gender-vs-Atk-DV, shiny-vs-DVs, Unown letter forme (DV formula, base Unown = A), Hidden-Power-type-vs-DVs with PS's maxed-IV auto-fill, duplicate moves, 18-char nicknames, species-name impersonation, zero-EV rejection. wasm bridge: **`Validator`** class — `validateTeam(teamJson)` / `canonicalizeTeam(teamJson)`, learnsets embedded (~13 KB gzipped; `Validator.fromJson` for external data). *Oracle cross-check* (`tools/validate-oracle.js`, wasm vs PS's real TeamValidator): all 34 pool + 120 fixture teams accepted by both with zero stray findings; 213 seeded mutations → **95.3% verdict agreement, the only 10 disagreements are the deliberately-injected unencodable move-combination probes (counted superset: we accept, PS rejects), zero reverse disagreements** (we never reject a PS-legal team); canonicalize certified on all mutants — 87 canonicalized teams re-validated through PS, 0 canonicalizer breaks.
    **M14b — import UI + open-sheet arena measurement: DONE (2026-07-17).** *Parser* (`web/src/ps-import.ts`): the standard PS teambuilder export format — multi-mon blocks (`Nick (Species) (M) @ Item` headers, Level/Shiny/Happiness/EVs/IVs/Nature/`- Move` lines, `Hidden Power [Ice]` brackets, `~` bullets, blank/`---` separators, `===` team headers → default save name, CRLF/BOM/indentation tolerated) — a faithful mirror of PS's `sim/teams.ts` importer with two deliberate divergences, both surfaced as findings instead of silent mangling: garbage PS silently drops (unrecognized property lines, bad EV/IV chunks, a property line where a mon header belongs) becomes a line-anchored `ps-*` error, and a missing `Level:` stays absent so the validator's `level-default` fix fills 55 (PS's importer force-fills 100, which this format would then reject). *Import UI* (team select screen, both locales, touch-first): paste → parse → `canonicalizeTeam` first ("fix it for me"): applied fixes listed as informational notes, remaining errors localized and anchored to mon/slot or paste line (`web/src/findings.ts` — the full validate.rs + parser code catalogue in en/ja, dex names through the M13 tables); on success saved to localStorage (`custom-teams.ts`), customs listed beside the pool teams (custom badge, two-tap delete), the bot's side stays pool-only. *Play path:* a custom team carries no pool index — the pair-table fetch is skipped, the worker pins the parsed team (`pinOpponent`, the M12 machinery — it never assumed pool membership), bot preview falls back to the live pinned search, battle hides picks only; rematch snapshots the sets, so deleting the saved team is safe. *Arena measurement* (the M14 gate item): new spec `open[:iters[:c[:buckets]]]` — `OpenAgent` (crates/bot), the M12 product policy in native form: the blind machinery with the opponent's true sets pinned (`Belief::pinned_from_battle`, certified ≡ `Belief::pinned` by bit-identical determinizations across whole games in tests/belief.rs) plus `open_preview_pick` baked previews; outer battles log-ON like `blind`. Measured vs perfect info including picks (equal budgets, 300 games, seed 1): **open:3000 vs skuct:3000 = 0.500 ± 0.057 on the 120-team fixture pool** (the free off-meta/custom-like population; think 1011 vs 972 ms/move) and **0.453 ± 0.056 on the meta pool** (in-distribution reference; 1032 vs 1020 ms/move). Reading: in-distribution, open-sheet ≈ blind-with-identified-sets (Gate A's 0.443 ± 0.056) — hiding picks alone costs a real but small ≈ −33 Elo; on the off-meta population the deficit disappears entirely (random fixture teams have less coherent pick synergy for perfect-info knowledge to exploit), and there is **no fixture-pool gap — off-meta sets do not hurt the determinized search itself**. Existing agents bit-identical (skuct:300 vs maxdamage seed 1 = 14W 6L; Gate B green; wasm parity 3-fixture bit-exact on the rebuilt binaries). *Verified* on the built dist with Playwright (en + ja): a PS-export paste with an injected illegal move + broken HP DV → localized, anchored errors (en "#3 Snorlax · Can't learn spikes" / ja 「#3 カビゴン · まきびしは覚えられません」, team-level clauses included); fixing the move imports with the applied fixes listed (HP DV derived, gender from Atk DV, EV/ability/nature fills); the saved team persists across reload, plays a **full game to the outcome banner** with the 30k think chip flipping to ponder at a live-search preview (no pair table for a custom matchup), zero console errors; two-tap delete persists across reload.
15. **M15 — PS interop: DONE (2026-07-17)**: (a) local sim-stream harness — the bot drives PS's own `BattleStream` (no networking) for cross-validation and games against PS-side bots; (b) websocket client (login, challenges, `|request|` handling) whose one genuinely new component is a **protocol→state importer**: the own side is fully known from request JSON; the opponent side has exactly M10's observer/belief/determinizer information structure (foe HP arrives at 1/48 granularity — imputed; purity is a declared non-goal), generalized to construct battles from public information instead of overwriting a known true state. Genuinely-hidden-set play (the parked full-blind policy) becomes the product policy here. Main-ladder botting requires PS staff permission; own-server/local play is unrestricted.
    **M15a — local sim-stream harness + protocol→state importer: DONE (2026-07-17).** *Importer* (`crates/bot/src/import.rs`): `ProtocolTracker` parses the player-visible protocol (the vocabulary is closed — this engine emits it bit-exactly) into per-mon public state: HP (own exact / foe 1/48 pixels, imputed mid-bucket), status **with the public counters** (sleep turns, Rest's 2-turn clock, the Stadium `residualdmg` Toxic counter — which survives cures/Rest-replacement and is removed only by switching, mirrored exactly; `brnattackdrop`/`parspeeddrop` lifecycles incl. boost-triggered and Haze removal), boosts (Baton Pass transfers them + copyable volatiles), announced volatiles (durations clamped by publicly elapsed turns; encore/disable move ids captured; perish counts; bind traps ending under their move's name), move-implied volatiles (Defense Curl/Minimize/Rage/Destiny Bond/Fury Cutter streak), side conditions + weather (upkeep-counted remaining duration), and **PP marks with `run_move`'s exact deduction rule** — charge-turn `|move|` lines deduct, release turns don't, thrash-class (2–3 actions) and Rollout (5 hits, whiff ends it) continuations don't, called moves (`[from]` another move) don't but Pursuit's `[from] Pursuit` switch-interception does, Spite/Mystery Berry adjust counts. `synthesize` then builds `Battle::from_fixture(own sets, belief refs)` and performs the state surgery: picks/positions (the M10 canonical party scheme; hidden opponent picks imputed — the determinizer resamples them per iteration), per-mon fields, engine-API planting (`set_status`/`add_volatile`/`add_side_condition`/`set_weather` — companion state and rolled hidden durations come from the real code paths), request bookkeeping via the engine's own `make_request` (faint `switch_flag`s, forced-switch counters), and the mid-turn queue `Belief::determinize`'s pending-move scrub expects (pending foe move + residual at Baton-Pass switch requests). Reused wholesale from M10: `Observer` (log channel, protocol-fed via new `from_mons`/`ingest_line`), `Belief` (filtering / fallback / pinned / determinize; new `refs()` accessor + one lenient rule: PS normalizes typed Hidden Powers to plain `hiddenpower`, so plain-HP reveals match any typed slot — typed M10 reveals unaffected, bit-identity preserved), and `BlindSearch` (unchanged loop; new optional root mask because **PS enforces Max Total Level = 155 at preview and the engine's enumeration did not** — at the time an accepted port gap, so preview picks (search root + baked-table mixed sampling) were projected onto the legal subset. *Addendum 2026-07-17: superseded at preview — the engine now enforces the 155 cap in validation and enumeration (see the M8 correction); the mask API and the filtered table sampling stay as harmless defense in depth*). `ProtocolAgent` orchestrates (also wasm `ProtocolSearcher`: `pushLines`/`onRequest`/`step`/`best`/`stateView`), with every submission projected onto the request-derived legal set (projection counter stayed 0 — the synthesized battle's own legality matched PS's at every decision). *Native certification* (`crates/bot/tests/import.rs`): all 60 conformance fixtures replayed per-side as a player would see them (`|split|`-filtered lines + requests built from own-side truth), both belief modes — **9,710 decision points, 0 public-field mismatches (own side exact incl. per-move PP; foe status/boosts exact + HP bucket + PP marks), 0 fixture choices illegal in the synthesized battle**; 22 residual volatile-set diffs remain as documented inference limits (mid-turn `twoturnmove` edges, Rage expiry nuance, Lock-On, single-turn Endure/stall at mid-turn switches, the foe's hidden Pursuit choice — correctly unknowable). *Harness* (`tools/ps-arena.js`, PS clone at `~/pokemon-showdown`): hosts `BattleStream`, our side driven purely from its player stream (request-before-update ordering handled by acting at stream quiescence), opponent = `RandomPlayerAI` (level-cap-aware preview subclass) or a max-base-power scripted player; every decision point also reads the omniscient `battleStream.battle` and asserts the synthesized state against the truth. **Gate a — protocol soundness: 110/110 complete games (blind vs random ×30 + max-BP ×15 at 300 iters, open-sheet ×15, plus the 50 gate-c games; pool + fixture opponent teams mixed, sides alternated), zero choice rejections, zero desyncs/timeouts.** **Gate b — state tracking: 2,410 decision points across those games, zero mismatches** (own side exact; opponent species/level/status/boosts exact, HP within the announced 1/48 bucket, revealed-move PP marks exact, side conditions + weather). **Gate c — strength vs `RandomPlayerAI` at `blind:1000`: 49W 1L 0T over 50 games** (mixed pool+fixture teams, seed 21, avg 16.4 turns). Existing behavior bit-identical: full native suite green (Gate B included), corpus test green, wasm parity/smoke/determinism green on the rebuilt binaries, arena `skuct:300` vs `maxdamage` seed 1 = 14W 6L, and `blind:300` vs `skuct:300` (meta pool, seed 1) identical before vs after (9W 11L, avg 16.7 — the M10c-quoted 15.8 predates the grown table set). *M15b still needs:* the websocket client proper — login/challenge flow, `|request|`+rqid handling over the wire, timers, reconnect/resume (the importer is already stream-agnostic: lines + request JSON in, choices out).
    **M15b — PS websocket client: DONE (2026-07-17).** *Client* (`tools/ps-client.js`, Node + `ws` — the only dependency, `tools/package.json`): connects to a PS server's `/showdown/websocket` (config: `--server` URL, `--name`, optional `--password` for registered accounts via the configured server's standard login endpoint; guest login = bare `/trn`, accepted by a `--no-security` server, with the `|challstr|` assertion flow against `--loginserver` as the fallback), sends or accepts challenges in `gen2nc2000` (`/utm` packed team — pool team by index / `pool:random` / a team file — then `/challenge`/`/accept`; challenge state is tracked from the modern `|pm|…|/challenge FORMAT|…` updates, an empty `/challenge` meaning resolved — this server no longer sends the legacy `|updatechallenges|` JSON, which is kept only as a fallback), and drives battles with M15a's wasm `ProtocolSearcher`: battle-room lines buffered (noise-filtered down to the M15a player-stream vocabulary), each `|request|` acted on after a 100ms quiescence debounce (the network analogue of the sim-stream idle wait; team-preview requests additionally hold until the `|poke|` lines land), choices submitted as `/choose <choice>|<rqid>` (server-side stale-request protection; client-side the newest request always wins). `|error|` recovery honors PS's two rejection kinds: `[Unavailable choice]` re-sends an updated request (the normal flow re-chooses); anything else leaves the request open with no re-send, so the client re-poses the last request itself and falls back to the always-legal `default` on repeat — every battle-room `|error|` counts against gate a regardless. Blind product policy by default; `--mode open` pins the opponent's true sets (`--opp-team-file`) for genuinely-open-sheet servers; `--random` turns the client into the uniform request-legal second driver (level-cap-aware at preview) for gate runs; `--games N` sequential battles, concurrency capped at 1. *Reconnect/resume:* on a socket drop the client reconnects, re-logs-in, and `/join`s every live battle; the server replays `|init|battle` + the full player-channel room log and then re-sends the open `|request|` (plus `|sentchoice|` if that request was already answered — the client then recomputes but withholds the re-send); the driver rebuilds a fresh searcher from the replayed log — the importer is stateless-rebuildable from full log + latest request — and the rebuild is **proven** by comparing the rebuilt `stateView` bit-for-bit against the pre-drop one whenever the rqid matches. *Verification* (local clone server, `node pokemon-showdown start --skip-build --no-security 8123`, all games complete over the real websocket): **gate a — 24 games at `blind:1000` vs the random driver: 23W 1L 0T, 339 decisions, 0 choice rejections, 0 desyncs/timeouts, 0 legality drift, 0 projections, max think 653ms (avg 395ms)**; **gate b — chaos socket kills (`--drop`) at team preview, mid-battle moves (incl. one right after the choice was sent), and forced-switch points: 7 drops across 7 games, every battle auto-resumed and finished with 0 rejections after resume; resume proofs 6/6 bit-identical wherever the pre-drop request was still open on rejoin, 1 skipped by design (the post-choice drop advanced the turn, so rqids differ)**; **gate c — 6 games with `/timer on` (server-confirmed in each battle): 6W 0L, 0 timer losses, max think latency 603ms (avg 367ms) against a 150s+ budget**. Open-mode smoke (pinned sheets over the wire) clean. POLICY: botting on the main ladder (play.pokemonshowdown.com) requires PS staff permission — `--server` has no default, the restriction is stated in `--help`, and the client is meant for the local clone or an explicitly-configured self-hosted server.

### Roadmap (M16+ — verification-first)

Scope decisions (2026-07-21, settled with the owner after the first ladder exposure — real losses to a strong community player, the 2026-07-20 six-way audit, and the regulation migration + synthesis-bug fixes shipped in the second deploy):

- **M11 metagame research: CUT.** Proximate causes: the bake it fed on was ruled meaningless (exact-signature table lookup contributes zero against custom ladder teams — measured in both losses) and its fitness gauntlet came from the wrong regulation. Deeper cause is ordering: optimizing teams with a bot whose search/eval still has structural holes optimizes noise. The M11a machinery (teamgen operators, gauntlet fitness, `research_meta`, `bake_preview --candidate`) stays in-tree; re-decide inclusion only after the bot is complete and stably operating.
- **New goal: bot/eval sanity verification, then strengthening.** Measurement before fixes, fixes before features — three questions: (i) does the bot judge equity correctly, (ii) do its decisions track strong human play, (iii) what structural blind spots remain. Plug the measured holes; only then look for strength gains. Method lesson carried in: the theory audit predicted 11 eval divergences, measurement found 2 and refuted 1 — every question gets a harness first, and every later change re-runs the harnesses.
- Standing assets: `examples/damage_conformance.rs` (eval-vs-engine damage diff; 2 known bugs), the 570-battle / 12,226-turn human spectator corpus (87.9% of battles contain a condition the eval cannot see), the layered blind-spot map (L4 tables dead → L3 belief → **L2 rollout = the bottleneck** → L1 eval; fix L2 before L1 — the rollout decides which positions the eval is ever asked to score), the replay-postmortem harnesses, the M15 importer (replays protocol logs into engine states), and live ladder access for end-to-end re-exposure.

Milestones:

16. **M16 — bot/eval sanity verification.**
    - **M16-0 — anomaly repro (cheap; first):** the two ladder root choices ranked LAST by the bot's own eval (G1 T6 Dynamic Punch over Fire Blast; G1 T12 Hidden Power over strictly-dominating Earthquake) plus the slot-2-never-used-in-16-selections suspicion — `replay_postmortem` either confirms a move-slot mapping bug in the client choice path (critical, poisons all live play) or attributes them to visit noise under the (0.25,0.75) leaf squash (document and close). Also: the generic-`hiddenpower`-via-PS-protocol eval path, untested.
    - **M16a — equity calibration:** does `eval01` measure equity? Sample decision-point positions (self-play, plus real-game positions synthesized from the human corpus via the M15 importer), estimate ground-truth equity with high-budget **seed-marginal** playouts, report correlation / Brier / calibration curve **sliced by feature region** (Substitute, Spikes, sleep + counters, confusion, boosts incl. evasion, screens, weather, trapping, Perish) — blind-spot regions get located by measurement, not theory. Gate: a reproducible calibration report; the harness stays as a regression net beside `damage_conformance`.
    - **M16b — human-agreement:** replay the corpus through the importer; at each human decision point ask the product-config bot for its root policy; measure top-1/top-k agreement, overall and sliced (winner vs loser side, game phase, condition present, action class — attack/status/switch). Harness-design caveat: spectator logs carry no `|request|` JSON, so the acting side's unrevealed sets must be belief-imputed too (the structure the live bot already faces) — restrict or stratify by how revealed the acting set is. Deliverable is the **ranked disagreement-cluster list**, not one number (predicted first cluster: voluntary switching); divergence ≠ bot-wrong — agreement-with-the-winning-side is the directional signal.
    - **M16c — structural hole closure**, in the measured order L2 → L1 → L3; each fix gated by its harness + seed-paired arena regression, shipped under the standing pre-approval, then re-exposed on ladder:
      - **L2 rollout (`greedy_pick`):** voluntary switching (currently unreachable while any move exists), a non-zero policy for status moves (all score 0; sets with no scorable move play uniformly at random — 10/204 on the pre-migration pool, the skill cores), guaranteed-fail pruning (Rest at full HP was used four times in one lost game).
      - **L1 eval:** the two measured damage bugs — the bp=0 family (`return`/`frustration`/`flail`/`reversal`/`magnitude`/`present`/`counter`/`mirrorcoat`/`bide` all score 0.0; `return` alone lost game 3629) and Explosion/Selfdestruct missing the Def halving — then extend `damage_conformance` to perturbed positions (boosts/screens/weather/status: the harness samples turn-1 only, where the predicted weather / 1.1x-item / stat-rollover / 997-cap divergences cannot fire).
      - **L3 belief/import:** imputed foe movesets must retain revealed moves (the observed-Spikes-dropped case); off-pool fallback sets must never be empty (empty → Struggle → threat 0); seed fallback synthesis from the no-OHKO community-rentals DB rather than heuristics where possible.
    - **M16 exit gate:** all harnesses re-run green or explained after the fixes, plus a fresh ladder batch whose postmortem finds no blunder of an already-fixed structural class.
17. **M17 — strengthening (entered 2026-07-21; M16 harnesses quiet).** Rev 1 shipped (fec2e75): calibration-gated eval revision (sleep-clock scaling, Substitute bonus, status ×0.7; r 0.681→0.709, parity duels). Program re-planned with the owner (2026-07-21) under three premises: the human mistake-report channel is saturating (rising cost per finding — replace it with compute), custom parties are incoming, and longer thinking time is acceptable for quality. Order: **e → c → a/b → d** (e depends on nothing and supplies c's ground truth; a is retroactive over archived logs, so deferring it loses no data).
    - **M17e — exact endgame equity.** The one ground truth sharing no bias with eval or rollout: solve small endgame subgames exactly — simultaneous-move matrix games over enumerated chance nodes, Nash per state by RM+/LP, memoized backward over the forward closure, self-limiting state-count budget (Metronome-class blowups just exceed it). Anchors the eval tail (feeds c), can replace the rollout inside solved regions, and — root in region — plays exact endgames outright. **Step 1 SHIPPED:** `BattleRng` Oracle mode (all 37 in-battle PRNG sites are single-u32 interval partitions; class probabilities are exact rationals over 2^32), `battle::enumerate::enumerate_step` (lexicographic DFS over the draw trace; one engine run per chance leaf), and the `chance_conformance` example — gate: partitions sum to 2^32 exactly, leaf probabilities sum to 1 (worst err 8e-14), 51,300 seeded replays across 171 steps all land bit-exactly (by `state_key`) inside the enumeration, frequencies match (worst |z| 3.49 over 1,225 cells) — PASS. **Step 2 SHIPPED (dd6005d):** `bot::exact` — certified interval solver: horizon-limited backward induction, truncated lines carry [0,1], matrix-game values are monotone in payoffs so lo/hi matrices bracket the true value (compact tableau simplex, best-response-certified, worst gap 4e-15), iterative deepening with stall bail-out, persistent memo for fully-resolved subgames; `endgame_exactness` harness compares eval01 against the brackets. **Measured wall (12+40-game runs):** at 1v1 with healthy HP even horizon 1 costs ~2M engine runs (~100 s WSL) — the two-sided 39-damage-roll joint fan is real state splitting that merges only at overkill range, so tight certification is confined to lethal-range positions (my "1v1 will almost surely solve" estimate was wrong). Wide brackets keep value as one-sided tests, and the first run already **proved an eval error ≥ 0.235 with zero playouts** (eval 0.429 vs certified [0.663, 0.990], Clefable 26/220 vs Exeggutor 66/205 — the HP-tail miscalibration class M17c targets). **Step 3 SHIPPED (ea64dd9):** damage-roll range-merging (owner-approved criterion: coinciding endpoint subtrees — state_key + downstream trace — pin every interior roll via damage's monotonicity; bisect otherwise; `chance_conformance` re-PASS with 0/51,300 unmatched is the standing soundness gate) and `endgame_exactness_corpus` — positions from the 570-battle human corpus (owner decision: real games reach similar endgames), reconstructed via the M16b fabrication path, exact-for-the-imputed-determinization. First 100 battles: 7 attempts, 6 bracketed (4 solved exactly at 36–11k runs — merging collapses overkill fans 28×), and **all 6 are proven eval violations**: certain losses scored 0.354, certain wins scored 0.700–0.908, margins to 0.354 — the tail-saturation failure, now with zero-playout proof instances for M17c to calibrate against. **e-4 SHIPPED (13ba634,** per the in-tree `ENDGAME-SOLVER-ALGORITHM.md` proposal, items 1–3 of its sequence): honest run accounting (`StepEnum{leaves, runs}` — budgets use actual engine executions), **Phase A integer-damage quotienting** (`apply_damage_variance` branches over distinct final damages at the observable boundary — sound by construction; endpoint merge demoted to second layer), and 128-bit dual-hash identity (`state_key128`) for every certificate-carrying merge/memo. Gates: 23/23 green, `chance_conformance` PASS (skipped-by-cap 7→2, 0/52,800 unmatched). Corpus 0–99: aborted 1→0, tight 4→5, **7/7 attempts proven violations — new worst: eval 0.511 at a certified certain loss** (b93 Charizard 46/201 vs Starmie 34/169). **Full 570-corpus sweep DONE** (3 local shards, ~30 min; merged CSV `tmp/eec-all.csv`): 72 attempts → 57 bracketed → **38 certified-tight anchors** and **47 proven eval violations** (median margin 0.242, 12 ≥ 0.4, max **0.873** — Suicune 23/226 vs Charizard 100/201: eval 0.127, certified 1.000). Tail structure quantified against truth: exact∈[0,0.1) bias **+0.342**, exact∈[0.9,1] bias **−0.388** — the eval compresses decided tails by ~0.35 both ways, and the worst cases are *ordering* errors (loser ahead of winner), i.e. missing initiative/lethal-range ("who KOs whom first"), not a scale miscalibration. This anchor corpus is M17c's target set. **M17c rev 2 SHIPPED (5ac23d4):** heal-aware KO-race eval term at last-mon 1v1 (recharge/Substitute/sleep-vs-SleepTalk/freeze/residual-clock/heal-sustain aware; race=3.0), gated by the new `anchor_gate` harness (zero-noise regression vs the certified anchors, seconds/run — violations 47→38, margin 14.14→12.63, MAE 0.325→0.265) + calibration (r 0.725→0.741) + seed-paired duel (**B wins 0.555±0.069**). Measured method lesson: the heal-BLIND variant fit the anchors far better (MAE 0.138) and lost the duel at 0.39 — anchor fit is necessary-not-sufficient, the duel stays sovereign. **e-5 Phase B SHIPPED (b82f98a):** `bot::bounds` — persistent lazy bound graph with threshold certificate mode (stop at the first proof vs an eval±margin band) and hybrid cells (first visit tries the eager range-merged enumerator within `cell_cap`; only overflowing fans go lazy — measured essential). Measured: b93 Contained in **427 runs vs 987k** for the eager full solve, b91 proven in 65, b15 width-0 in 28; threshold mining also immediately caught a rev-2 eval bug (heal rule voiding a faster attacker's one-shot — fixed with the kill-now exemption; anchors 38→35 viol / MAE 0.186, duel exact parity 100-100). **e-5 memory steps SHIPPED:** guarded dead-damage semantic keys cut b455 expansions 11,730→4,156 and live nodes 25,848→22,005 at the same 200k runs and identical `[0.003,0.940]` bracket; terminal mass is now folded directly and exact non-root nodes move to a persistent closed memo. On b455, terminal folding reduced live nodes 22,652→16,477 and wall 24→21 s; at a 17k cap, closed folding completed 200k runs with 16,267 live nodes where the no-fold arm hit the cap at 192k/17,209. A guarded one-sided-heal rank (`PP total + non-healer HP + turns remaining`) validates every nonterminal edge and steers toward lower resource generations near the node cap; the 570-corpus endgame slice admitted 24/72 roots with zero invariant failures. Correction: b455 itself is two-sided healing (Snorlax Leftovers versus Skarmory Rest), so it is intentionally outside that classifier. Open e-5 frontier: certify support/BR pruning, then apply PP/resource/SCC solving to the residual two-sided-heal subgame. Phase C approximate product use stays gated.
    - **M17e Phase C addendum (supersedes the open-frontier sentence above): SHIPPED.** Support and pure best responses steer work without excluding unresolved actions; lower and upper witnesses alternate, a sparse legacy-support pass preserves fast lethal closures, and a 32-visit fair cursor prevents starvation. Pure rows/columns are permanently removed only by a cross-bound certificate (`U(candidate) ≤ L(dominator)` for every live opposing action); LP numeric gaps are included outward in root bounds. At 20k runs over corpus 0–100 this raised certified-tight anchors 3→5, proven violations 2→4, and reduced total runs 72,150→64,690. b455 changed from node-budget `[0.003,0.940]` at 191k runs to a proof `[0.004,0.865]` at 64,869 runs. Next: generalize the resource rank to two-sided healing, then solve the strategically reduced resource DAG. A nontrivial SCC solver is inapplicable while exact keys retain turn and every decision edge advances it.
    - **M17c — loss-region value discrimination** (the frozen stay-in postmortem, 29f67cd): sigmoid-tail compression flattens the root exactly where a human exploits free tempo (own equity 0.079 → all actions equal). Candidates: logit-space backup / (0.25,0.75) leaf-squash rework, free-turn (unanswered setup) tempo penalty. Truth source: M17e values. Gate: the frozen game flips to switch + calibration non-regression + parity duels.
    - **M17a — regret mining (the replacement report line):** re-search every decision point of live games + corpus with a high-budget **seed-marginal** oracle config; rank by equity regret, cluster, fix classes. Retroactive over archived logs; shards to cx. Honest limit: shares the eval's bias, so it catches search errors only — eval errors are covered by standing calibration/Brier on live games and by M17e.
    - **M17b — budget scaling curve:** strength vs thinking time (1×/2×/4×/8×, seed-paired duels); deploy at the knee. The same high-budget machinery doubles as M17a's oracle.
    - **M17d — off-pool robustness (custom parties):** close the L3 zero-reveal leftover (off-pool empty imputed sets); custom-party gauntlet built from the M11a teamgen assets; later, exploiter-team search (a team-level exploitability probe) as an automated attack line that needs no human reports.
    - Still live from the original list, re-ranked by the detectors above: eval features for the top corpus conditions (turn-weighted: Spikes 21.4%, sleep counter 20.1%, Substitute 9.2%, confusion 8.6%), weight re-tuning on the fixed eval (re-run M6's plateau conclusion, seed-paired), M16b cluster items. Standing gate for every change: seed-paired arena (`--ab` + `eval_ab_duel`) vs the M16-exit bot + ladder results against the community.

Parked (not scheduled, not dead-by-principle): M11 metagame research (re-decide after stable operation), preview-table baking in any form (owner-ruled meaningless 2026-07-21 — revisit only with a lookup that generalizes off-pool), M11 certification tables.

Non-goals: exploitation/opponent modeling, large NNs / GPU inference, minor-party *optimization* (custom parties are accepted since the 2026-07-17 decision, but priors/tables/evaluation still specialize on the meta pool), whole-game equilibrium solving, unpermissioned main-ladder botting. Longer-term verification ideas stay live: coverage-forcing corpora, expert scenario fixtures, predicted-vs-actual diffing during live play.

## Baseline measurements (this machine, WSL)

- PS (TS): 6.5 battles/s, 570 turns/s, 5.5 ms per clone → tree search is infeasible on the TS engine
- Target: 1e5–1e6 turns/s, sub-microsecond clones (prior art pkmn/engine claims >1000x over PS)
- Rust engine after M4 (`cargo run --release -p conformance --example bench`):
  54k turns/s log-off replay (35k log-on), 52k turns/s full-game random playouts,
  **103k turns/s clone-based playouts** (clone + reseed + play to the end — the MCTS
  determinization workload), 4.7 µs / 4 allocs per mid-battle clone (the 4 are the two
  roster Vecs + occasional volatile spill). That is ~95x PS on turns and ~1100x on clones.
  M4 history: 10k → 20k (integer event identity) → 24k (lookup hoisting + aggregate
  masks) → 31k (TypeId + log gating) → 41k (battle-level mask gate) → 52k (zero-handler
  fast path + ActiveMove diet); clones 22 µs/153 allocs → 4.7 µs/4 allocs (compact
  Copy state). Profile is flat now (top item ~9%); see `examples/profile.rs`
  (flamegraph) and `examples/clone_anatomy.rs` (per-field clone costs).

## Porting landmines (facts measured in this repo)

1. **The sim runs whatever ability it is handed, even in Gen 2.** The validator's canonical form is `ability: 'No Ability'`. A blank ability round-tripped through pack/unpack gets default-filled with the species ability, and e.g. Shed Skin then fires and changes battle outcomes (proven on battle-005). All fixtures are TeamValidator-clean.
2. **Gen 2 DV consistency**: SpA DV must equal SpD DV; the HP DV is derived from the low bits of Atk/Def/Spe/Spc. The validator rejects teams that violate this.
3. **After team preview, PS truncates `side.pokemon` to the 3 picked mons** — snapshot party size goes 6 (preview) → 3 (battle).
4. Replay must reconstruct players from the inputLog's `>player` lines (packed teams) or it will not match the live battle (the generator does this).
5. The PRNG is the Gen 5 64-bit LCG. `random(n)` is `(next * n) >> 32`, exactly matching JS float math for n < 2^21 (debug_assert enforced).

## References

- Reference implementation: `PS_ROOT` (default `~/pokemon-showdown`, `node build` required). Data provenance commit: `meta.psCommit` in `data/gen2stadium2.json`.
- Measured scope: 267 moves (88 with callbacks) / 62 items (38) / 37 conditions / 0 abilities / 251 species (246 after the 5 Ubers), 377 callbacks total, 76 distinct event hooks in use.

## Licensing

This repository's code is MIT (see [`LICENSE`](LICENSE)). `data/gen2stadium2.json`, `fixtures/`, `reference/`, and `PORTING.md` are derived from [Pokemon Showdown](https://github.com/smogon/pokemon-showdown) (MIT); the meta team pool transcribes credited public tournament/community data. Full attribution, including the Pokemon IP notice (names/mechanics only — no sprites, art, sound, or ROM assets anywhere in the tree): [`THIRD-PARTY-NOTICES.md`](THIRD-PARTY-NOTICES.md).
